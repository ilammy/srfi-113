language: c

sudo: requires

before_install:
  - mkdir -p /tmp/scratch

install:
  - ./tools/install-chibi
  - ./tools/install-dependencies
  - ./tools/install-self

script:
  - ./tools/run-all-tests
